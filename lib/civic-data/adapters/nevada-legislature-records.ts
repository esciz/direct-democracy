import { createPlaceholderAdapter } from "@/lib/civic-data/adapters/base";

export const nevadaLegislatureRecordsAdapter = createPlaceholderAdapter({
  key: "nevada-legislature-records",
  displayName: "Nevada legislative record dataset",
});
