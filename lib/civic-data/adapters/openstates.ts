import { createPlaceholderAdapter } from "@/lib/civic-data/adapters/base";

export const openStatesAdapter = createPlaceholderAdapter({
  key: "openstates",
  displayName: "OpenStates Nevada API",
});
