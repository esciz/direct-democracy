import { createPlaceholderAdapter } from "@/lib/civic-data/adapters/base";

export const asunAdapter = createPlaceholderAdapter({
  key: "asun",
  displayName: "Associated Students of the University of Nevada",
});

